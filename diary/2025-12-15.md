---
tags:
  - diary
---
# 주요 작업 및 결과
- bdz
	- 도커 컴포즈 대부분의 클라우드에서 지원 안함
	- 클라우드 서버는 k8s 사용 - 인테그레이션 때 k8s기반 런타임 필요
- k8s 공부
	- 네트워크
		- 전달 순서
			- 프로세스 > 소켓 > 커널 > 라우팅 > 인터페이스 > NIC > 네트워크 
				- 수신은 라우팅 없음
		- 전달되는 데이터
			- 데이터 $\in$ TCP/UDP 세그먼트 $\in$ ip 패킷 $\in$ 이더넷 프레임
			- 프레임 
				- src/dst MAC
				- ip 패킷
			- ip 패킷
				- src/dst ip
				- tcp/udp 세그먼트
			- tcp/udp 세그먼트
				- src/dst port
				- 데이터
		- 프로세스
			- 소켓 생성
			- 도착 ip/port 설정
			- 조건부로 출발 ip/port 설정
		- 커널
			- 송신
				- 소켓 버퍼 읽기
				- 목적지 확인 - ip/port
				- 소켓에 출발지 포트 할당
				- 출발지 ip 결정
					- 목적지로 라우팅 테이블 조회 - 인터페이스 선택
					- 인터페이스 ip로 출발지 할당
				- dst/next-hop MAC 조회 - arp
				- L2 + L3 + L4 헤더 붙여 프레임 생성
				- NIC로 전송
			- 수신
				- ip 주소 확인 - 인터페이스에 있는지
				- dst port/protocol로 디멀티플렉싱
				- 소켓에 데이터 쓰기
		- NIC
			- MAC 기반 통신만 가능
				- MAC 주소로 송신
				- MAC 맞는 패킷 수신
		- 라우팅 테이블
			- `{dst ip-prefic/network-bit} via {next-hop ip-addr} dev {interface}`
			- next-hop = gateway - 중간 전달 라우터
			- 매칭
				- long prefix matching - 앞에서부터 제일 긴 ip로 매칭
				- metric 낮은 순 - prefix 같을 때 
				- default - 0.0.0.0/0
	- 파일시스템
		- backing fs
			- persistent
			- 실제 파일 시스템
			- 디스크의 데이터
		- 런타임 뷰
			- vfs 객체 집합
				- mount - 특정 dentry에 다른 파일시스템 트리(superblock + dentry)를 끼워넣는 것
				- dentry - 파일 이름
				- inode - 파일 내용
				- superblock - 디스크에서 읽은 파일시스템 메타데이터
	- 프로세스
		- 프로그램의 인스턴스
			- 메모리 공간에서의 실행 파일
			- 열린 파일 디스크립터(fd)
				- 파일, 소켓, 파프, 디바이스등 자원에 대한 핸들
			- 레지스터 상태 - 실행 지점
			- pid 식별 객체
		- pid
			- 커널이 프로세스 식별하는 키
			- 부모-자식관계를 ppid이용해 정의 
			- pid 1
				- 최상위 부모 프로세스
				- orphan 프로세스에 대한 sigchld 처리 및 좀비 처리 
		- 자원 - 커널이 소유/관리, 프로세스에 할당/회계/중재될 수 있는 대상
	- 컨테이너
		- 이미지에서 생성된 프로세스
		- 호스트와 다른 커널 객체 집합을 참조
		- 구성요소 
			- 프로세스 
				- 이미지에 정의된 프로그램의 인스턴스
			- 커널 객체
				- namespace 
					- 자원에 대한 식별과 가시성
					- 프로세스에서 커널에 자원 요청시 프로세스의 네임스페이스 따라 커널이 자원 해석범위 결정
					- 상세
						- PID 
							- 컨테이너 내에서 자신은 1
							- 호스트 pid 접근 불가
						- mount
							- 이미지 레이어에 대해 overlayfs로 구성된 vfs뷰가 mount namespace에 귀속
								- merged에 upper와 lower 합쳐져서 보임
							- 읽기 전용 이미지 레이어
								- lowerdir에 저장
								- 프로그램, 루트 파일 시스템
								- copy on write
									- 파일 쓰기가 발생할 때, 쓰기 레이어로 해당 부분 복사
									- copy-up
							- 쓰기 레이어
								- upperdir에 저장
									- 컨테이너 제거 시 삭제 
								- 변경을 위한 작업 공간
								- 컨테이너마다 유일
						- network 
							- 인터페이스, ip, 라우팅 테이블 분리 
						- UTS - 호스트 이름 격리
						- IPC - 통신 자원 격리 - 공유 메모리, 세마포어, 메시지 큐
						- UID 
							- 사용자 권한 분리 - 필수 아님
							- 도커는 기본 off - 컨테이너 탈출시 UID 그대로
							- rootless
								- 호스트 일반유저에 내부 유저 맵핑
								- mount syscall 불가 - 마운트 권한 없음
								- 커널의 권한 판단은 호스트 기준			
									- bind 마운트는 권한 불일치로 파일 접근 불가
									- 유저 공간 트릭을 사용하나 성능/호환 손해
									- chonw 불가 - 호스트파일 접근 불가
								- idmapped
									- 볼륨에 접근할때만 uid/gid 해석을 마운트 단위 맵핑
									- 호스트가 맵을 만들어 컨테이너 mountspace에 제공
				- cgroup 
					- 자원에 대한 제약 - cpu, memory, io, pid 수
					- 프로세스의 자원 회계 담당
	- OCI - Open Container Initiative
		- 명세
			- 런타임 명세 - 컨테이너 실행: 실행 설정 / cli명령어
			- 이미지 명세 - 컨테이너 생성 정보: 파일 규격
				- index - 환경별 사용할 menifest
				- menifest 
					- 사용할 layer, config에 대한 메타데이터 
					- sha256으로 참조
				- config 
					- 이미지 제작과 실행 정보 
					- 엔트리포인트, 커맨드, 작업디렉토리, 환경변수
				- layer 
					- tar로 압축된 file diff 
						- 변경은 파일단위 교체
						- 삭제는 삭제 지시
					- 위에서 아래로 덮어씀
			- 배포 명세 - http api: 이미지 레지스트리
# 한담
- 학습을 위한 트리기반 ux-컨텍스트 관리 llm 인터페이스가 절실함
	- 중간 내용 질문하다가 이전 질문 까먹어버리는 경우(내가)가 너무 많음
	- 페이지나 카드별로 묶어서 추가 질문/용어 질문/페이지 병합 정도의 기능이 필요할 듯 
		- 페이지 선택 후 추가질문/용어질문하면 연결 되어있는 상위 노드들을 모두 컨텍스로 묶어서 전송
		- 페이지 병합은 컨텍스트에서 분리되는 별개의 트리 노드를 생성
			- 새로운 컨텍스트 루트
			- 해당 페이지를 기반으로 설명 진행