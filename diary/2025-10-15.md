---
tags:
  - diary
---
# 주요 작업 및 결과
- [[NVIDIA AI ACADEMY]]
	- Getting Started with AI on Jetson Nano
		- jetson nano
			- 실제 모델 명은 jetson tx2 
			- ssh를 통한 로컬 접속
				- `ssh <id>@<ip_address>`
					- user와 id 가 세팅 되어 있어야 함
					- jetson 기본 값
						- ip 
							- usb 연결시 `192.168.55.1`
							- 이후 ifconfig로 이더넷 연결 주소 확인
						- id - nvidia
						- password - nvidia
- ssh 접속
	- `ssh <id>@<ip_address>`
		- 접속하려는 대상에 이미 사용자/비밀번호 정보가 있어야 함
		- `ssh-keygen -t ed25519  -f ~/.ssh/id_ed25519`으로 키 생성
			- git 등의 프로그램에서는 표준 키만 검색
			- 로컬에서는 passphrase 필요 없음
				- passphrase 설정 시 ssh-agent로 관리하면 편함
					- 해당 세션 동안 passphase유지
		- `ssh-copy-id -i <pubkey_file_name> <id>@<ip_address>`로 키 전달 가능
			- 중간에 로그인 필요


# 한담
- 
