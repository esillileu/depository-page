---
tags:
  - diary
---
# 주요 작업 및 결과
- [[NVIDIA AI ACADEMY]]
	- 리눅스 
		- hyper-V 기반 wsl 이용하여 수업 진행 
			- [[Virtualization|가상화]] 기술
				- 하이퍼바이저
					- 하드웨어 수준 가상화
					- 하이퍼 바이저 위에 독립된 운영체제의 가상머신 생성
					- os 포함 
					- 완벽한 격리
				- 컨테이너
					- 운영체제 수준 가상화
					- 호스트와 커널 공유 - 애플리케이션과 라이브리 격리
					- os 미포함으로 가볍고 빠름
					- 격리 수준 낮음
		- [[Shebang|쉬뱅]]
			- 실행 파일 최상단에 실행 인터프리터 지정
			- 권한을 줄 필요가 있음 - [[chmod]]
		- [[LTS]] - long term support
	- wsl 활성화 
		- `windows 기능 켜기/끄기`에서 다음 체크 후 재시작
			- `Hyper-V`
			- `Linux용 Windows 하위 시스템`
			- `virtual machine platform`
		- 터미널에서 다음 실행 
			```powershell
			wsl --update
			wsl --list --online 
			wsl --install [release_name]
			```
	- 도커 설치
		- 그냥 wsl에 직접 설치해서 돌리는걸로
			```bash
			sudo apt update -y
			sudo apt upgrade -y
			sudo apt install -y apt-transport-https ca-certificates curl software-properties-common
			curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
			echo "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
			sudo apt update -y
			sudo apt install -y docker-ce docker-ce-cli containerd.io
			sudo usermod -aG docker $USER
			sudo service docker start

			```

# 한담
- 리눅스
	- 뭔가 이미 하던 내용이라 어렵진 않았다. 설정이랑 이런 걸 이미 다 해버려서 좀 당황스럽긴 하지만
